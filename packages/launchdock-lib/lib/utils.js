
/**
 *  Launchdock Utils
 */

Launchdock.utils = {};


/**
 * Slugify a string
 * @param {String}
 */
Launchdock.utils.slugify = function (str) {
  // underscorestring:underscore.string
  return s.slugify(str);
}
